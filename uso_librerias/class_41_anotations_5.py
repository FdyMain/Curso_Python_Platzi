from typing import Union

def process_salary(salary: Union[int, float]) -> float:
    """
    Procesa un salario que puede ser enterno o flotante y lo devuelve como flotante.

    Parámetros:
    salay(Union[int, float]): Un salario que puede ser enterno o flotante

    Retorna:
    float: el salatio convertido a flotante
    """

    return(salary)